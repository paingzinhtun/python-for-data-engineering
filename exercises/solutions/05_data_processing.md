# Module 05 Solutions

## Beginner

```python
amount = float("125.50")
country = raw_country.strip().upper() if raw_country.strip() else "UNKNOWN"
```

## Intermediate
Required field validation should return missing field names or a boolean with reason details.

## Applied
Build a new dictionary per record with trimmed IDs, converted numeric values, and standardized country codes.

## Scenario
Validate first, transform second, and keep rejected rows with reasons so data quality issues remain visible.
